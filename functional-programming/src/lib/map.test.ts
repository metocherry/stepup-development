import { assertEquals } from "https://deno.land/std/testing/asserts.ts";

import map from "./map.ts";


Deno.test("if data is array, creates an array of values", () => {
  // arrange
  const data = [1, 2, 3];

  const d1 = new Set([1, 2, 3]);

  // act
  // const actual = map<number, number>(d1, (value) => value);
});

